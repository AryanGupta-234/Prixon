requests>=2.31.0
python-dotenv>=1.0.1

# --- local semantic understanding ---
# FastEmbed/BGE is the primary action and memory retrieval mechanism.
fastembed>=0.3.0

# --- optional: real launch verification and system awareness ---
psutil>=5.9.0

# --- optional cloud provider SDKs (not used in local-only test mode) ---
huggingface_hub>=0.34.0

# --- optional voice ---
SpeechRecognition>=3.10.0
PyAudio>=0.2.14
pyttsx3>=2.90
edge-tts>=6.1.0
sounddevice>=0.4.6
numpy>=1.26.0
